export const config = {
  apiPath: '/api',
  defaultView: 'tickets',
  events: {
    LOGOUT: 'logout',
    LOAD_MAIN_VIEW: 'load-main-view',
    SHOW_OVERLAY: 'show-overlay',
    HIDE_OVERLAY: 'hide-overlay'
  }
}
